Markov Chain Monte Carlo(MCMC) interface/functions in `Haskell`

The basic idea is to take advantages of `Haskell`'s typeclass feature, so that provide a common interface for a variaty of MCMC algorithms.